<style>
    body {
        margin: 0;
        overflow: hidden;
        background-color: black; /* Set background color to black */
    }

    #background-video {
        width: 100vw;
        height: 100vh;
        z-index: -1;
        object-fit: cover;
    }

    .pixel {
        width: 9px;
        height: 9px;
        position: absolute;
        background-color: black;
        pointer-events: none;
        transition: background-color 0.2s ease-out;
        z-index: 0;
    }

    /* Text container for desktop */
    .text-container-desktop {
        display: none; /* Keep desktop text container hidden */
        position: absolute;
        z-index: 1;
        font-family: Courier, monospace;
        font-size: 12px;
        color: white;
        width: 30%;
        top: 50%;
        left: 20%;
        transform: translate(-50%, -50%);
        text-align: left;
    }

       .text-container {
        display: none; /* Keep mobile text container hidden */
        position: absolute;
        z-index: 1;
        font-family: Courier, monospace;
        font-size: 10px;
        color: white;
        width: 80%;
        top: 150vh;
        left: 50%;
        transform: translate(-50%, -50%);
        text-align: center;
    }

    /* Styles for mobile */
    @media only screen and (max-width: 600px) {
        .pixel {
            width: 18px;
            height: 18px;
        }

        #background-video {
            width: 100vw;
            height: 70vh; /* Adjust height as needed */
        }
    }
</style>
